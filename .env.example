# Intentionally incomplete starter values for local development.
# Students must adapt these for Google Cloud deployment.

APP_ENV=development
PORT=8000

# Cloud SQL (PostgreSQL)
DB_HOST=
DB_PORT=5432
DB_NAME=
DB_USER=
DB_PASSWORD=

# Google Cloud
GOOGLE_CLOUD_PROJECT=
GCS_BUCKET_NAME=
FIRESTORE_COLLECTION_AUDIT_EVENTS=

# Optional local credentials path (do not commit real credentials)
GOOGLE_APPLICATION_CREDENTIALS=
